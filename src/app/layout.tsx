import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Header } from '@/components/layout/header'
import { Footer } from '@/components/layout/footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ATS Alumni Association - Hong Kong Aberdeen Technical School',
  description: 'Hong Kong Aberdeen Technical School Alumni Association - Connecting alumni worldwide, fostering professional growth, and supporting our alma mater.',
  keywords: ['Aberdeen Technical School', 'Alumni', 'Hong Kong', '香港仔工業學校', 'ATS'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  )
}